apiVersion: v1
kind: Service
metadata:
  name: {{ include "argo-practice.fullname" . }}
  labels:
    {{- include "argo-practice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "argo-practice.selectorLabels" . | nindent 4 }}
