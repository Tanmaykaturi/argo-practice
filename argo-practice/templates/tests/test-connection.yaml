apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "argo-practice.fullname" . }}-test-connection"
  labels:
    {{- include "argo-practice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "argo-practice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
