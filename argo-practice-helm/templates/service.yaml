apiVersion: v1
kind: Service
metadata:
  name: {{ include "argo-practice-helm.fullname" . }}
  labels:
    {{- include "argo-practice-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "argo-practice-helm.selectorLabels" . | nindent 4 }}
