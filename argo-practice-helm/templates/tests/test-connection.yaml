apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "argo-practice-helm.fullname" . }}-test-connection"
  labels:
    {{- include "argo-practice-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "argo-practice-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
